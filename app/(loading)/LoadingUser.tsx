import { Text } from 'react-native-paper';
import { Background } from '../../widgets';

export default function LoadingUser() {
  return (
    <Background>
      <Text variant='displayLarge'>LoadingUser..</Text>
    </Background>
  );
}
