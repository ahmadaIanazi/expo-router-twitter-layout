/* Just toggle True of false or to turn off or on * /
/* === ( OFF / ON ) === */
export const providers_config = {
  analytics_provider: true,
  auth_provider: true,
  development_provider: true,
  firebase_provider: true,
  hooks_provider: true,
  imports_provider: true,
  pushNotification_provider: true,
  Query_provider: true,
  Theme_provider: true,
  Update_provider: true,
  tamagui_provider: true,
};