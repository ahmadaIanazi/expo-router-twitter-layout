import { Text, View } from 'react-native'

const NullScreen = () => {
  return (
    <View style={{ flex: 1 ,justifyContent: 'center', alignItems: 'center' }}>
      <Text>Null Screen</Text>
    </View>
  )
}

export default NullScreen
